import { AuthWitnessProvider } from '@aztec/aztec.js/account';
import { CompleteAddress, GrumpkinPrivateKey } from '@aztec/circuit-types';
import { Fr } from '@aztec/foundation/fields';
import { DefaultAccountContract } from '../defaults/account_contract.js';
/**
 * Account contract that authenticates transactions using Schnorr signatures
 * verified against a Grumpkin public key stored in an immutable encrypted note.
 */
export declare class SchnorrAccountContract extends DefaultAccountContract {
    private signingPrivateKey;
    constructor(signingPrivateKey: GrumpkinPrivateKey);
    getDeploymentArgs(): Fr[];
    getAuthWitnessProvider(_address: CompleteAddress): AuthWitnessProvider;
}
//# sourceMappingURL=account_contract.d.ts.map