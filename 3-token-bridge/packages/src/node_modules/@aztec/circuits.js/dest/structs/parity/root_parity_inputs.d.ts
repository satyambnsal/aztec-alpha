/// <reference types="node" resolution-mode="require"/>
import { BufferReader, Tuple } from '@aztec/foundation/serialize';
import { NUM_BASE_PARITY_PER_ROOT_PARITY } from '../../constants.gen.js';
import { RootParityInput } from './root_parity_input.js';
export declare class RootParityInputs {
    /** Public inputs of children and their proofs. */
    readonly children: Tuple<RootParityInput, typeof NUM_BASE_PARITY_PER_ROOT_PARITY>;
    constructor(
    /** Public inputs of children and their proofs. */
    children: Tuple<RootParityInput, typeof NUM_BASE_PARITY_PER_ROOT_PARITY>);
    toBuffer(): Buffer;
    static fromBuffer(buffer: Buffer | BufferReader): RootParityInputs;
}
//# sourceMappingURL=root_parity_inputs.d.ts.map