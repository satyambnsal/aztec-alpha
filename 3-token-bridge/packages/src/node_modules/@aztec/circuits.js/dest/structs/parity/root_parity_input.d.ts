/// <reference types="node" resolution-mode="require"/>
import { BufferReader } from '@aztec/foundation/serialize';
import { FieldsOf } from '@aztec/foundation/types';
import { Proof } from '../proof.js';
import { ParityPublicInputs } from './parity_public_inputs.js';
export declare class RootParityInput {
    /** The proof of the execution of the parity circuit. */
    readonly proof: Proof;
    /** The public inputs of the parity circuit. */
    readonly publicInputs: ParityPublicInputs;
    constructor(
    /** The proof of the execution of the parity circuit. */
    proof: Proof, 
    /** The public inputs of the parity circuit. */
    publicInputs: ParityPublicInputs);
    toBuffer(): Buffer;
    static from(fields: FieldsOf<RootParityInput>): RootParityInput;
    static getFields(fields: FieldsOf<RootParityInput>): readonly [Proof, ParityPublicInputs];
    static fromBuffer(buffer: Buffer | BufferReader): RootParityInput;
}
//# sourceMappingURL=root_parity_input.d.ts.map