/// <reference types="node" resolution-mode="require"/>
import { Fr } from '@aztec/foundation/fields';
import { BufferReader, FieldReader, Tuple } from '@aztec/foundation/serialize';
import { FieldsOf } from '@aztec/foundation/types';
import { MAX_NEW_L2_TO_L1_MSGS_PER_CALL, MAX_NEW_NOTE_HASHES_PER_CALL, MAX_NEW_NULLIFIERS_PER_CALL, MAX_NOTE_HASH_READ_REQUESTS_PER_CALL, MAX_NULLIFIER_KEY_VALIDATION_REQUESTS_PER_CALL, MAX_NULLIFIER_READ_REQUESTS_PER_CALL, MAX_PRIVATE_CALL_STACK_LENGTH_PER_CALL, MAX_PUBLIC_CALL_STACK_LENGTH_PER_CALL, NUM_FIELDS_PER_SHA256, RETURN_VALUES_LENGTH } from '../constants.gen.js';
import { Header } from '../structs/header.js';
import { SideEffect, SideEffectLinkedToNoteHash } from '../structs/side_effects.js';
import { CallContext } from './call_context.js';
import { L2ToL1Message } from './l2_to_l1_message.js';
import { NullifierKeyValidationRequest } from './nullifier_key_validation_request.js';
import { ReadRequest } from './read_request.js';
/**
 * Public inputs to a private circuit.
 * @see abis/private_circuit_public_inputs.hpp.
 */
export declare class PrivateCircuitPublicInputs {
    /**
     * Context of the call corresponding to this private circuit execution.
     */
    callContext: CallContext;
    /**
     * Pedersen hash of function arguments.
     */
    argsHash: Fr;
    /**
     * Return values of the corresponding function call.
     */
    returnValues: Tuple<Fr, typeof RETURN_VALUES_LENGTH>;
    /**
     * The side-effect counter under which all side effects are non-revertible.
     */
    minRevertibleSideEffectCounter: Fr;
    /**
     * Read requests created by the corresponding function call.
     */
    noteHashReadRequests: Tuple<SideEffect, typeof MAX_NOTE_HASH_READ_REQUESTS_PER_CALL>;
    /**
     * Nullifier read requests created by the corresponding function call.
     */
    nullifierReadRequests: Tuple<ReadRequest, typeof MAX_NULLIFIER_READ_REQUESTS_PER_CALL>;
    /**
     * Nullifier key validation requests created by the corresponding function call.
     */
    nullifierKeyValidationRequests: Tuple<NullifierKeyValidationRequest, typeof MAX_NULLIFIER_KEY_VALIDATION_REQUESTS_PER_CALL>;
    /**
     * New note hashes created by the corresponding function call.
     */
    newNoteHashes: Tuple<SideEffect, typeof MAX_NEW_NOTE_HASHES_PER_CALL>;
    /**
     * New nullifiers created by the corresponding function call.
     */
    newNullifiers: Tuple<SideEffectLinkedToNoteHash, typeof MAX_NEW_NULLIFIERS_PER_CALL>;
    /**
     * Private call stack at the current kernel iteration.
     */
    privateCallStackHashes: Tuple<Fr, typeof MAX_PRIVATE_CALL_STACK_LENGTH_PER_CALL>;
    /**
     * Public call stack at the current kernel iteration.
     */
    publicCallStackHashes: Tuple<Fr, typeof MAX_PUBLIC_CALL_STACK_LENGTH_PER_CALL>;
    /**
     * New L2 to L1 messages created by the corresponding function call.
     */
    newL2ToL1Msgs: Tuple<L2ToL1Message, typeof MAX_NEW_L2_TO_L1_MSGS_PER_CALL>;
    /**
     * The side effect counter at the start of this call.
     */
    startSideEffectCounter: Fr;
    /**
     * The end side effect counter for this call.
     */
    endSideEffectCounter: Fr;
    /**
     * Hash of the encrypted logs emitted in this function call.
     * Note: Represented as an array of 2 fields in order to fit in all of the 256 bits of sha256 hash.
     */
    encryptedLogsHash: Tuple<Fr, typeof NUM_FIELDS_PER_SHA256>;
    /**
     * Hash of the unencrypted logs emitted in this function call.
     * Note: Represented as an array of 2 fields in order to fit in all of the 256 bits of sha256 hash.
     */
    unencryptedLogsHash: Tuple<Fr, typeof NUM_FIELDS_PER_SHA256>;
    /**
     * Length of the encrypted log preimages emitted in this function call.
     * Note: Here so that the gas cost of this request can be measured by circuits, without actually needing to feed
     *       in the variable-length data.
     */
    encryptedLogPreimagesLength: Fr;
    /**
     * Length of the unencrypted log preimages emitted in this function call.
     */
    unencryptedLogPreimagesLength: Fr;
    /**
     * Header of a block whose state is used during private execution (not the block the transaction is included in).
     */
    historicalHeader: Header;
    /**
     * Chain Id of the instance.
     *
     * Note: The following 2 values are not redundant to the values in self.historical_header.global_variables because
     * they can be different in case of a protocol upgrade. In such a situation we could be using header from a block
     * before the upgrade took place but be using the updated protocol to execute and prove the transaction.
     */
    chainId: Fr;
    /**
     * Version of the instance.
     */
    version: Fr;
    constructor(
    /**
     * Context of the call corresponding to this private circuit execution.
     */
    callContext: CallContext, 
    /**
     * Pedersen hash of function arguments.
     */
    argsHash: Fr, 
    /**
     * Return values of the corresponding function call.
     */
    returnValues: Tuple<Fr, typeof RETURN_VALUES_LENGTH>, 
    /**
     * The side-effect counter under which all side effects are non-revertible.
     */
    minRevertibleSideEffectCounter: Fr, 
    /**
     * Read requests created by the corresponding function call.
     */
    noteHashReadRequests: Tuple<SideEffect, typeof MAX_NOTE_HASH_READ_REQUESTS_PER_CALL>, 
    /**
     * Nullifier read requests created by the corresponding function call.
     */
    nullifierReadRequests: Tuple<ReadRequest, typeof MAX_NULLIFIER_READ_REQUESTS_PER_CALL>, 
    /**
     * Nullifier key validation requests created by the corresponding function call.
     */
    nullifierKeyValidationRequests: Tuple<NullifierKeyValidationRequest, typeof MAX_NULLIFIER_KEY_VALIDATION_REQUESTS_PER_CALL>, 
    /**
     * New note hashes created by the corresponding function call.
     */
    newNoteHashes: Tuple<SideEffect, typeof MAX_NEW_NOTE_HASHES_PER_CALL>, 
    /**
     * New nullifiers created by the corresponding function call.
     */
    newNullifiers: Tuple<SideEffectLinkedToNoteHash, typeof MAX_NEW_NULLIFIERS_PER_CALL>, 
    /**
     * Private call stack at the current kernel iteration.
     */
    privateCallStackHashes: Tuple<Fr, typeof MAX_PRIVATE_CALL_STACK_LENGTH_PER_CALL>, 
    /**
     * Public call stack at the current kernel iteration.
     */
    publicCallStackHashes: Tuple<Fr, typeof MAX_PUBLIC_CALL_STACK_LENGTH_PER_CALL>, 
    /**
     * New L2 to L1 messages created by the corresponding function call.
     */
    newL2ToL1Msgs: Tuple<L2ToL1Message, typeof MAX_NEW_L2_TO_L1_MSGS_PER_CALL>, 
    /**
     * The side effect counter at the start of this call.
     */
    startSideEffectCounter: Fr, 
    /**
     * The end side effect counter for this call.
     */
    endSideEffectCounter: Fr, 
    /**
     * Hash of the encrypted logs emitted in this function call.
     * Note: Represented as an array of 2 fields in order to fit in all of the 256 bits of sha256 hash.
     */
    encryptedLogsHash: Tuple<Fr, typeof NUM_FIELDS_PER_SHA256>, 
    /**
     * Hash of the unencrypted logs emitted in this function call.
     * Note: Represented as an array of 2 fields in order to fit in all of the 256 bits of sha256 hash.
     */
    unencryptedLogsHash: Tuple<Fr, typeof NUM_FIELDS_PER_SHA256>, 
    /**
     * Length of the encrypted log preimages emitted in this function call.
     * Note: Here so that the gas cost of this request can be measured by circuits, without actually needing to feed
     *       in the variable-length data.
     */
    encryptedLogPreimagesLength: Fr, 
    /**
     * Length of the unencrypted log preimages emitted in this function call.
     */
    unencryptedLogPreimagesLength: Fr, 
    /**
     * Header of a block whose state is used during private execution (not the block the transaction is included in).
     */
    historicalHeader: Header, 
    /**
     * Chain Id of the instance.
     *
     * Note: The following 2 values are not redundant to the values in self.historical_header.global_variables because
     * they can be different in case of a protocol upgrade. In such a situation we could be using header from a block
     * before the upgrade took place but be using the updated protocol to execute and prove the transaction.
     */
    chainId: Fr, 
    /**
     * Version of the instance.
     */
    version: Fr);
    /**
     * Create PrivateCircuitPublicInputs from a fields dictionary.
     * @param fields - The dictionary.
     * @returns A PrivateCircuitPublicInputs object.
     */
    static from(fields: FieldsOf<PrivateCircuitPublicInputs>): PrivateCircuitPublicInputs;
    /**
     * Deserializes from a buffer or reader.
     * @param buffer - Buffer or reader to read from.
     * @returns The deserialized instance.
     */
    static fromBuffer(buffer: Buffer | BufferReader): PrivateCircuitPublicInputs;
    static fromFields(fields: Fr[] | FieldReader): PrivateCircuitPublicInputs;
    /**
     * Create an empty PrivateCircuitPublicInputs.
     * @returns An empty PrivateCircuitPublicInputs object.
     */
    static empty(): PrivateCircuitPublicInputs;
    isEmpty(): boolean;
    /**
     * Serialize into a field array. Low-level utility.
     * @param fields - Object with fields.
     * @returns The array.
     */
    static getFields(fields: FieldsOf<PrivateCircuitPublicInputs>): readonly [CallContext, Fr, [Fr, Fr, Fr, Fr], Fr, [SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect], [ReadRequest, ReadRequest], [NullifierKeyValidationRequest], [SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect, SideEffect], [SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash, SideEffectLinkedToNoteHash], [Fr, Fr, Fr, Fr], [Fr, Fr, Fr, Fr], [L2ToL1Message, L2ToL1Message], Fr, Fr, [Fr, Fr], [Fr, Fr], Fr, Fr, Header, Fr, Fr];
    /**
     * Serialize this as a buffer.
     * @returns The buffer.
     */
    toBuffer(): Buffer;
    /**
     * Serialize this as a field array.
     */
    toFields(): Fr[];
    hash(): Fr;
}
//# sourceMappingURL=private_circuit_public_inputs.d.ts.map